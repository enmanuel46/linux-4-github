name: free vps 100% no virus 2027
run-name: ${{ github.actor }} is running a VPS.
on: workflow_dispatch
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sSf https://sshx.io/get | sh -s run
