name: Ubuntu LTS
run-name: ${{ github.actor }} is running Ubuntu.
on: workflow_dispatch
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sSf https://sshx.io/get | sh -s run
