name: Ubuntu 26.04 LTS
run-name: ${{ github.actor }} is running Ubuntu 26.04 LTS.
on: workflow_dispatch
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-26.04
    steps:
      - run: curl -sSf https://sshx.io/get | sh -s run
